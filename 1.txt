<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>BSU-SEC-2025-001 | Security Advisory — mis.bsu.edu.eg</title>
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800&family=JetBrains+Mono:ital,wght@0,300;0,400;0,600;0,700;1,400&family=Bebas+Neue&display=swap" rel="stylesheet">
<style>
/* ─── TOKENS ─────────────────────────────────── */
:root{
  --bg0:#05080f;
  --bg1:#080d18;
  --bg2:#0c1220;
  --bg3:#101829;
  --bg4:#141e33;
  --line:rgba(255,255,255,0.06);
  --line2:rgba(255,255,255,0.1);

  --blue:#2563eb;
  --blue2:#3b82f6;
  --blue3:#60a5fa;
  --cyan:#06b6d4;
  --teal:#14b8a6;
  --red:#ef4444;
  --red2:#dc2626;
  --red3:#fca5a5;
  --amber:#f59e0b;
  --amber2:#fcd34d;
  --green:#10b981;
  --green2:#6ee7b7;
  --purple:#8b5cf6;
  --purple2:#c4b5fd;

  --text:#e2e8f8;
  --text2:#94a3b8;
  --text3:#64748b;

  --mono:'JetBrains Mono',monospace;
  --sans:'Tajawal',sans-serif;
  --display:'Bebas Neue',sans-serif;

  --sidebar:260px;
  --r:8px;
}

*{margin:0;padding:0;box-sizing:border-box;}
html{scroll-behavior:smooth;font-size:15px;}

body{
  font-family:var(--sans);
  background:var(--bg0);
  color:var(--text);
  overflow-x:hidden;
}
::-webkit-scrollbar{width:5px;background:var(--bg1);}
::-webkit-scrollbar-thumb{background:var(--blue);border-radius:3px;}

/* ─── SIDEBAR ─────────────────────────────────── */
#sidebar{
  position:fixed;right:0;top:0;
  width:var(--sidebar);height:100vh;
  background:var(--bg1);
  border-left:1px solid var(--line);
  display:flex;flex-direction:column;
  z-index:200;
  overflow:hidden;
}
.sb-top{
  padding:20px 18px 14px;
  border-bottom:1px solid var(--line);
  flex-shrink:0;
}
.sb-id{
  font-family:var(--mono);font-size:9px;
  letter-spacing:3px;color:var(--cyan);
  margin-bottom:6px;
}
.sb-name{font-size:14px;font-weight:700;line-height:1.3;color:var(--text);}
.sb-badges{display:flex;gap:5px;flex-wrap:wrap;margin-top:8px;}
.sb-badge{
  font-family:var(--mono);font-size:8px;letter-spacing:1px;
  padding:2px 7px;border-radius:3px;border:1px solid;
}
.sb-badge.conf{color:#fca5a5;border-color:rgba(239,68,68,.35);background:rgba(239,68,68,.1);}
.sb-badge.disc{color:#6ee7b7;border-color:rgba(16,185,129,.35);background:rgba(16,185,129,.1);}
.sb-badge.active{color:var(--cyan);border-color:rgba(6,182,212,.35);background:rgba(6,182,212,.1);}

.sb-body{flex:1;overflow-y:auto;padding:8px 0 20px;}
.sb-body::-webkit-scrollbar{width:3px;}
.sb-body::-webkit-scrollbar-thumb{background:var(--bg4);}

.sb-group{padding:14px 0 4px;}
.sb-group-title{
  font-family:var(--mono);font-size:8px;letter-spacing:3px;
  color:var(--text3);padding:0 18px 6px;text-transform:uppercase;
}
.nav-a{
  display:flex;align-items:center;gap:10px;
  padding:8px 18px;text-decoration:none;
  font-size:12.5px;color:var(--text2);
  cursor:pointer;border-right:2px solid transparent;
  transition:all .15s;
  position:relative;
}
.nav-a:hover{color:var(--text);background:rgba(255,255,255,.04);}
.nav-a.on{color:var(--text);background:rgba(59,130,246,.08);border-right-color:var(--blue2);}
.nav-a .sev{
  font-family:var(--mono);font-size:8px;letter-spacing:1px;
  padding:1px 5px;border-radius:2px;margin-right:auto;
}
.sev-c{background:rgba(239,68,68,.2);color:#fca5a5;border:1px solid rgba(239,68,68,.3);}
.sev-h{background:rgba(245,158,11,.2);color:#fcd34d;border:1px solid rgba(245,158,11,.3);}
.sev-m{background:rgba(16,185,129,.2);color:#6ee7b7;border:1px solid rgba(16,185,129,.3);}

/* ─── TOPBAR ──────────────────────────────────── */
#topbar{
  position:fixed;top:0;left:0;
  right:var(--sidebar);
  height:48px;
  background:rgba(5,8,15,.9);
  border-bottom:1px solid var(--line);
  backdrop-filter:blur(20px);
  display:flex;align-items:center;
  padding:0 40px;gap:16px;
  z-index:100;
}
.tb-dot{width:7px;height:7px;border-radius:50%;background:var(--red);animation:blink 2s infinite;}
@keyframes blink{0%,100%{opacity:1}50%{opacity:.3}}
.tb-title{font-family:var(--mono);font-size:11px;color:var(--text2);letter-spacing:1px;}
.tb-sep{width:1px;height:16px;background:var(--line2);}
.tb-tag{
  font-family:var(--mono);font-size:9px;letter-spacing:2px;
  padding:2px 8px;border-radius:3px;
}
.tb-tag.red{color:#fca5a5;background:rgba(239,68,68,.12);border:1px solid rgba(239,68,68,.25);}
.tb-tag.amber{color:#fcd34d;background:rgba(245,158,11,.12);border:1px solid rgba(245,158,11,.25);}
.progress{height:2px;position:absolute;bottom:0;left:0;width:0;background:linear-gradient(90deg,var(--blue),var(--cyan));transition:width .2s;}

/* ─── MAIN ────────────────────────────────────── */
#main{
  margin-right:var(--sidebar);
  padding-top:48px;
}

/* ─── SECTIONS ────────────────────────────────── */
.sec{
  padding:70px 56px;
  border-bottom:1px solid var(--line);
  max-width:960px;
}
.sec:last-child{border-bottom:none;}
